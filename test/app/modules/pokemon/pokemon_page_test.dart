import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('PokemonPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PokemonPage(title: 'Pokemon')));
    //  final titleFinder = find.text('Pokemon');
    //  expect(titleFinder, findsOneWidget);
  });
}
